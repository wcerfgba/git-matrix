describe('HeatmapFeature', () => {
  describe('activate', () => {
    it('binds window change handler', () => {
    
    })

    it('creates a HeatmapEditor for each visible editor', () => {
    
    })
  })
  
  describe('vscode.window.onDidChangeVisibleTextEditors', () => {
    it('deactivates all existing HeatmapEditors', () => {
    
    })
  })
})